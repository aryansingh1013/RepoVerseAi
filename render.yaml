services:
  - type: web
    name: repoverse-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/theme
